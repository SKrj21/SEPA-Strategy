# Stride Hook Configuration

Hooks execute on your local machine at key points in the task lifecycle.
Each hook receives: TASK_ID, TASK_IDENTIFIER, TASK_TITLE, AGENT_NAME as environment variables.

## before_doing
Runs before claiming a task (60s timeout). Typically used to pull latest code.

```bash

```

## after_doing
Runs after implementation is complete (120s timeout). Typically used to run tests/lint.

```bash

```

## before_review
Runs when task enters review (60s timeout). Typically used to open a pull request.

```bash

```

## after_review
Runs after approval (60s timeout). Typically used to merge and deploy.

```bash

```
